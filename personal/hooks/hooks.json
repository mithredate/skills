{
  "description": "Privileged commands are Mehrdad's to run: block aws, aws-vault, kubectl, helm, terraform apply/destroy and hand him the command.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/guard-privileged-commands.mjs\""
          }
        ]
      }
    ]
  }
}
